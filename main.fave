fun sayHi(first, last) {
    var repeat = 10;
    // print (first + " " + last + " ") * repeat;

    for (var i = 0; i < repeat; i = i + 1) {
        print first + " " + last + " ";
    }
}

sayHi("Fabian", "Reader");